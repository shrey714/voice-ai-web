# Overnight Improvement Loop — worklog

Started 2026-07-08 (night). User asked: loop through the customer-facing website start→end,
make it the best it can be, keep going all night. Wants to wake up surprised.

## Ground rules (MUST hold every iteration)
- After EVERY batch: `npx tsc --noEmit` clean + `npm run build` exit 0.
- Lint error count must stay ≤ **18** (current baseline). Count:
  `npx eslint src 2>&1 | grep -cE "^\s+[0-9]+:[0-9]+\s+error"`
- No production DB migrations (C1/C2 deferred by user). No touching real Supabase data.
- No auto-preview server. Verify via tsc/build only.
- Semantic tokens only, lucide icons only, shadcn via CLI only. Keep mock/seeded data (policy).
- Small, safe, genuine improvements. Don't churn already-excellent code for its own sake.

## Customer journey (start → end)
1. `/` home (page.tsx) — landing/shop list
2. `/[slug]` shop catalog (ShopClient.tsx)
3. `/[slug]/product/[productId]` product detail (ProductDetailClient.tsx)
4. `/[slug]/checkout` checkout
5. `/[slug]/order/[orderId]` order tracking
6. `/orders` order history
7. `/wishlist`
8. `/addresses`, `/auth`
9. static: `/about /help /privacy /terms`, Footer, BottomNav, error/not-found

## Iterations log
(newest first)

### Iter 5 — 2026-07-08 night (cron loop)
- **PWA manifest was stale on every field** — still old branding "Shopkeeper Online" / short_name "Shop" / old Sage-cream `#F6F5F1` bg / old teal `#215B66`. App is now **ShopNear** on a white light-background (layout themeColor `#ffffff`, primary `oklch(0.52 0.105 223.128)` = `#007595`). Rewrote `public/manifest.json`: name/short_name/description now match `SITE_NAME` + the app's real meta description, `background_color` → `#ffffff` (splash no longer flashes cream), `theme_color` → computed brand teal `#007595`, added `"purpose": "any"` to the icon. Fixes the wrong name/color shown in the Android install prompt + splash screen. (`public/manifest.json`)
- Verified: manifest parses as valid JSON; tsc clean, lint 18, build green.

### Iter 4 — 2026-07-08 night (cron loop)
- **Static-page SEO — canonical URLs:** the 4 content pages (`about`, `help`, `terms`, `privacy`) exported metadata but set no canonical, unlike the shop/product pages. Added self-referential `alternates: { canonical: '/…' }` to each (prevents duplicate-content ambiguity from query strings / trailing slashes). Confirmed the `<link rel="canonical">` tags render in the prerendered HTML.
- **Legal copy polish:** Terms + Privacy "Last updated" read a bare "2026" (looked like a placeholder) → "July 2026" (static by design; legal dates shouldn't track the clock). (`terms/page.tsx`, `privacy/page.tsx`)
- Verified: tsc clean, lint 18, build green.

### Iter 3 — 2026-07-08 night (cron loop)
- **Wishlist → Add to cart:** wishlist cards previously only opened the product or removed it — no way to buy. Added an "Add to cart" button per card (uses `addItemsToCart(item.slug, …)` since wishlist spans shops; toast offers "View shop"). Card restructured to the stretched-link pattern (focusable whole-card "view product" target at z-10 under the add/remove buttons at z-20) so no interactive nesting. Button bottom-aligns via `flex-1` + `mt-auto`. (`wishlist/page.tsx`)
- Verified: tsc clean, lint 18, build green.

### Iter 2 — 2026-07-08 night (main session)
- **Auth icon badge:** the sign-in/verify icon sat in a wrapper with dead `rounded-2xl` and no size/bg — gave it a real `size-16 bg-primary/10 text-primary` badge so it reads as intentional. (`auth/page.tsx`)
- **Design-system consistency:** replaced the raw `🎉` emoji in checkout's "You save" line with a lucide `PartyPopper` (system is lucide-only, no UI emoji). (`checkout/page.tsx`)
- **SEO:** added `itemCondition: NewCondition` to the product Offer JSON-LD (rich-result eligibility, fully static — no fabricated data). (`[slug]/product/[productId]/page.tsx`)
- Verified: tsc clean, lint 18, build green.

### Iter 1 — 2026-07-08 night (main session)
- **Reorder (order tracking):** "Order Again" now actually re-adds all order items to the shop's cart + toast, then lands on the shop. (`[slug]/order/[orderId]/page.tsx`)
- **Help FAQ accuracy bug:** removed two answers that told customers to find "the phone number on your order tracking page" — no shop phone is shown anywhere (Shop type/DB has none). Rewrote to accurate guidance about live tracking + auto-cancel of unconfirmed orders. (`help/page.tsx`)
- **Clear cart w/ undo:** cart sheet now has a "Clear cart" control (snapshot + Undo toast). (`CartSheet.tsx`)
- **Reorder from Orders list:** each order card got a "Reorder" button. Card restructured button→div with a stretched, focusable "view order" target (z-10) under the Reorder button (z-20) to avoid nested interactives. New imperative `addItemsToCart()` helper in `lib/cart.ts`. (`orders/page.tsx`, `lib/cart.ts`)
- Verified: tsc clean, lint 18, build green (twice).

### Iter 0 — baseline
- tsc clean, lint 18, build green. Established.
